package model;

public class Directory extends Repository_Object {

    public Directory( String repositoryName,
                      String parent_path,
                      String path,
                      int size) {
        super(repositoryName, parent_path, path, size);
    }

}
